# 発表ガイド — 10 分デモの順序と想定 Q&A

## 主張（1 文）

教科書の知識グラフ（地図）を実験で接地し、生徒が **納得の度合いを根拠つきで自分の地図に記録する** と、
「知っている」の深さと来歴が見える化され、理解・納得感・意欲を高める手がかりになる。

## 10 分の流れ

| 時間 | 見せるもの（ノートの節／図） | 言うこと |
|:--|:--|:--|
| 0:00 | 表題の表 | 「知っている」は一様ではない。まだ／聞いた／たしかめた／説明できる／証明できる。この 5 段階を **印** と呼ぶ |
| 1:00 | §1 `ref_map` | 教科書の 14 ノード。事実は定まる。矢印は **並べ方の一案**。だから一致度で採点しない |
| 1:45 | §2 `map_0_after_class` | 授業直後の A さん。発展以外は全部「聞いた」（白）。発展 2 つは点線（まだ） |
| 2:30 | 実験1 `sieve_grid` → `mark()` の出力 | 自分で消して素数が残る。その瞬間に印が上がり、**1 行の出力に「わけ」と「根拠：実験1」が残る** |
| 3:30 | 実験3 `factor_trees` → `map_1_after_exp3` | 360 を 3 通りに分けても葉は同じ。2000 まで全部同じ。それでも一意性は **薄い灰色（たしかめた）のまま**。「2000 まで確かめたことは 2001 のことを何も言っていない」 |
| 4:30 | 実験4 `h_trees` | 4n+1 の世界では 441 = 21×21 = 9×49。「ただ一通り」は当たり前ではなく、証明が要る性質 |
| 5:15 | 実験5 `divisor_grid` → ⚠ の出力 | 約数の個数は説明できた。でもその説明は一意性を **借りている**。地図には黒の点線が残る |
| 6:00 | 実験7 の表 → 出力 | ユークリッドの証明で「素数は無限にある」が黒（証明できる）に。証明が使う素因数分解も 4 に上げ直す。**証明は地図の上を伝わる** |
| 6:45 | 実験8 `prime_race` | 26860 まで 3 余りが優勢、26861 で初めて逆転。観察 ≠ 証明を体で感じる |
| 7:30 | §4 `map_2_final_diff` / `history` | 二重枠と ↑ が成長。**ノードは 1 つも増えていない**。深くなっただけ。数ではなく深さ |
| 8:30 | §5 `ask_me()` の出力 | AI は地図を作らない。**問うだけ**。問いは地図の状態から規則で決まる（LLM 不要）。LLM を使うなら役目を「根拠を尋ねる係」に固定 |
| 9:30 | 設計メモの表 | 7 つの原則との対応。限界：教室未検証。次は放課後学習室で紙版（カード＋印のシール）から |

3 分版：§1 → 実験3 → §4 の差分 → `ask_me()`。

## そのまま使える一言

- 「地図は AI が作らない。作るのは生徒。AI は問うだけ」
- 「2000 まで確かめたことは、2001 について何も言っていない」
- 「441 = 21 × 21 = 9 × 49。『分解はただ一通り』は当たり前ではない」
- 「ノードは 1 つも増えていない。それでも 2 つが黒になり、5 つが濃い灰色になった。これが成長」
- 「約数の個数の説明は一意性を借りている。借りていることが地図に見える」
- 「矢印は正解ではなく、教科書の並べ方。だから採点しない」
- 「上げないことも記録する。正直さが土台」

## スライドに貼る図（すべてモノクロ PNG・200 dpi。GitHub Pages の `figures/` にある：https://jxta.github.io/kg-grounding-proto/figures/ref_map.png など）

| ファイル | 内容 |
|:--|:--|
| `ref_map` | 教科書の地図（14 ノード、発展は点線） |
| `map_0_after_class` | 授業のあと（発展以外は全部「聞いた」） |
| `sieve_grid` | 実験1 篩の表（残った素数は太枠、消した素数を右下に） |
| `factor_trees` | 実験3 360 の因数の木 3 通り |
| `map_1_after_exp3` | 実験1〜3 のあと（二重枠＝授業のあとからの変化） |
| `h_trees` | 実験4 H の世界の 441 の木 2 通り |
| `divisor_grid` | 実験5 72 の約数の表（4 × 3） |
| `prime_race` | 実験8 素数レース（全体と 26861 付近の拡大） |
| `map_2_final` | すべての実験のあと |
| `map_2_final_diff` | 実験1〜3 のあとからの変化（二重枠と ↑、借りている黒点線） |
| `history` | 3 回の記録の縮小版（名前なし） |

## 想定 Q&A

**Q. 概念マップと何が違う？**
従来の概念マップは静的な一枚の図で、教師の正解マップとの一致度で採点されがち。ここでは (a) ノードに印と根拠がつく、(b) 記録の差分で成長を見る、(c) 教科書の地図は背景として重ねるだけで採点しない。Open Learner Model の「根拠を伴う編集」に近い。

**Q. 採点しないなら、効果はどう測る？**
地図は評価対象ではなく学習者の道具。効果は転移課題・説明課題・動機づけ尺度で別に測る。「実験＋地図」「実験のみ」「通常授業」の 3 群を想定し、実験の効果と地図の効果を分ける。ノード数や印の平均は成果指標にしない。

**Q. AI はどこで使う？**
問う側だけ。`ask_me()` は規則で動き、借りている説明 → 発展の予想 → 「たしかめた」止まりの根拠 → 「聞いた」のままへの実験の勧め → 方法の理由 → わけのない矢印 → 定義の自分の言葉 → 説明の最初の一文 → 教科書にあって自分にない道、の順に種類を順ぐりに問う。LLM を使う場合も指示文で「地図を作り直さない・答えを言わない・根拠を尋ねる」を固定する。LLM 生成マップより自作マップのほうが理解に寄与するという知見に沿う。

**Q. 印は誰が判定する？**
本人。「わけ」と「根拠」を書かないと上がらない。先生の確認は `my_map.json` のログで後追いできる。弱い材料を使った説明は `check()` が ⚠ を出す。

**Q. 一意性は中1 で証明できないのに扱うのか？**
「たしかめた」で止めてよい。「2000 まで試した」を「証明できる」にしないこと、そして実験4 で「当たり前ではない」まで持っていければ十分。証明は高校以降で上げる楽しみが残る。

**Q. 実験4・7・8 は難しすぎない？**
発展扱い。単元本体は実験1・2・3・5・6。放課後学習室のように余裕がある場面向け。

**Q. JupyterLite で本当に動く？**
依存は sympy・networkx・matplotlib で、いずれも Pyodide に含まれる。日本語フォントは `fonts/` を同梱すれば図に出る。保存した地図はブラウザの中に残る。授業配備では `files/` にノートと `fonts/` を置く。

**Q. 紙でもできる？**
できる。最初は紙（ノードのカード＋印のシール＋根拠の付箋）から始め、Notebook は実験と記録のために使う段階的導入を想定。

**Q. 別の単元にも使える？**
道具セルの `UNIT` 辞書（ノードと矢印）を差し替えるだけ。ノードは 10〜20 個に絞る。実験が作りにくい単元（証明の単元など）は向かない。
